import './style.css';

type ClockComponentProps = {
    // Define tus propiedades aquí
};

const ClockComponent = ({ }: ClockComponentProps) => {
    return (
        <div>
            <p>ClockComponent</p>
        </div>
    );
}

export default ClockComponent;