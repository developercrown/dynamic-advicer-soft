import './style.css';

type QRComponentProps = {
    // Define tus propiedades aquí
};

const QRComponent = ({ }: QRComponentProps) => {
    return (
        <div>
            <p>QRComponent</p>
        </div>
    );
}

export default QRComponent;