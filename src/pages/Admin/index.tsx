import './style.css';

type AdminProps = {
    // Define tus propiedades aquí
};

const Admin = ({ }: AdminProps) => {
    return (
        <div>
            <p>Admin</p>
        </div>
    );
}

export default Admin;